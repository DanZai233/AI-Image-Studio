# Shared model envs for Vercel serverless functions
SHARED_PROVIDER_PASSWORD="your-shared-password"
SHARED_PROVIDER_API_KEY="your-provider-api-key"
SHARED_PROVIDER_ENDPOINT="https://api.openai.com/v1"
SHARED_PROVIDER_CHAT_MODEL="gpt-4o-mini"
SHARED_PROVIDER_IMAGE_MODEL="gpt-image-1"

# Public runtime branding (safe to expose in Vite)
VITE_APP_NAME="LUMINA Atelier"
VITE_SHARED_PROVIDER_LABEL="Studio Shared Model"
VITE_SHARED_PROVIDER_DESCRIPTION="Use the site preset model without configuring a personal API key."
VITE_SUPPORT_EMAIL=""
VITE_IMAGE_REFERENCE_HINT="Reference images are forwarded when the upstream image model accepts multimodal prompt content; otherwise the app falls back to contextual text guidance."
